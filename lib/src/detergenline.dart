
import 'package:flutter/material.dart';

class Detergenline extends StatefulWidget {
  const Detergenline({Key? key}) : super(key: key);

  @override
  State<Detergenline> createState() => _DetergenlineState();
}

class _DetergenlineState extends State<Detergenline> {
  @override
  Widget build(BuildContext context) {
    //double screenWidth = MediaQuery.of(context).size.width;
    //double screenHeight = MediaQuery.of(context).size.height;
    return Scaffold(
      body: Column(),
    );
  }
}